# Canonical site URL used for metadata, sitemap.xml, robots.txt and OG tags.
# TODO: set this to the confirmed production domain before deploying.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: enables the contact form to actually send email via Resend.
# Leave unset to keep the form's graceful "mail not configured" fallback.
RESEND_API_KEY=
RESEND_FROM_EMAIL=
RESEND_TO_EMAIL=zeeshnhmd.1@gmail.com
